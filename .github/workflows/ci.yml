name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  docs-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate SCM artifacts
        run: python .github/scripts/validate_scm.py
